pub fn run() {
    println!("unrecoverable_errors.rs");

    // Unrecoverable Errors with panic
    let vec = vec![1];
    // vec[2]; // index out of bounds; unrecoverable error
    // panic!("crash and burn"); // force unrecoverable error
    
}
